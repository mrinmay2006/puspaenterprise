# Upload this package to GitHub

The root contains the verified `index.html`.

For Cloudflare Pages + GitHub, keep this structure exactly:
index.html
sitemap.xml
README.md
schema.sql
functions/api/orders.js

Do not put Razorpay Secret Key in index.html. Real payment verification will be connected server-side.
